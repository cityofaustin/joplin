version: 2.1

jobs:
  test:
    docker:
      - image: cityofaustin/circleci-test:40936de
    steps:
      - checkout
      - run:
          name: "Test that CLI utilities work"
          command: bash ./.circleci/cli-test.sh
  build:
    docker:
      - image: cityofaustin/circleci-build:e56c99c
    steps:
      - checkout
      - setup_remote_docker:
          version: 18.09.3
      - run: echo "placeholder for build"
      # - run:
      #     name: "Build Joplin Docker Image"
      #     command: bash ./.circleci/build.sh
  backup_database:
    docker:
      - image: cityofaustin/circleci-test:40936de
    steps:
      - checkout
      - run:
          name:
          command: bash ./.circleci/backup_database.sh
  push_to_heroku:
    docker:
      - image: cityofaustin/circleci-build:e56c99c
    steps:
      - checkout
      - run: echo "placeholder for push_to_heroku"
  migrate:
    docker:
      - image: cityofaustin/circleci-build:e56c99c
    steps:
      - checkout
      - run: echo "placeholder for migrate"

workflows:
  version: 2.1
  build-and-deploy:
    jobs:
      - test
      - build:
          requires:
            - test
      - backup_database:
          requires:
            - test
      # - backup_database:
      #     filters:
      #       branches:
      #         only:
      #           - master
      #           - production
      - push_to_heroku:
          requires:
            - build
            - backup_database
      - migrate:
          requires:
            - push_to_heroku
